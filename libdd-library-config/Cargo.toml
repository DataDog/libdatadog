# Unless explicitly stated otherwise all files in this repository are licensed under the Apache License Version 2.0.
# This product includes software developed at Datadog (https://www.datadoghq.com/). Copyright 2021-Present Datadog, Inc.

[package]
name = "libdd-library-config"
version = "1.1.0"
description = "Configuration management library for Datadog products."
homepage = "https://github.com/DataDog/libdatadog/tree/main/libdd-library-config"
repository = "https://github.com/DataDog/libdatadog/tree/main/libdd-library-config"
edition = "2021"
license.workspace = true

[lib]
crate-type = ["lib"]
bench = false

[features]
default = ["std"]
std = [
    "serde/std",
    "anyhow/std",
    "yaml_serde/std",
    "dep:prost",
    "dep:rand",
    "dep:rmp",
    "dep:rmp-serde",
    "dep:libdd-trace-protobuf",
    "dep:memfd",
    "dep:rustix",
]
otel-thread-ctx = []

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
# TODO: Switch to official crates.io release once no_std support is merged:
# https://github.com/yaml/yaml-serde/pull/7
yaml_serde = { git = "https://github.com/pawelchcki/yaml-serde.git", rev = "c7ad78def628d372dca2d9435b3ff0621a464e97", default-features = false }
prost = { version = "0.14.1", optional = true }
anyhow = { version = "1.0", default-features = false }
thiserror = { version = "2", default-features = false }

rand = { version = "0.8.3", optional = true }
rmp = { version = "0.8.14", optional = true }
rmp-serde = { version = "1.3.0", optional = true }

libdd-trace-protobuf = { version = "3.0.1", path = "../libdd-trace-protobuf", optional = true }

[dev-dependencies]
tempfile = { version = "3.3" }
serial_test = "3.2"

[target.'cfg(unix)'.dependencies]
memfd = { version = "0.6", optional = true }
rustix = { version = "1.1.3", features = ["param", "mm", "process", "fs", "time"], optional = true }
