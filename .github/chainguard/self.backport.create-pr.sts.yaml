issuer: https://token.actions.githubusercontent.com

subject: repo:DataDog/libdatadog:pull_request

claim_pattern:
  event_name: (pull_request_target|pull_request)
  ref: ".+"
  job_workflow_ref: DataDog/libdatadog/\.github/workflows/backport\.yml@.+

permissions:
  contents: write
  pull_requests: write
