name: '[rust] Cache' 
description: '[rust] Cache'

runs:
  using: composite
  steps:
    - name: ⚡ Cache
      uses: actions/cache@v2
      with:
        path: |
          ~/.cargo/registry/
          ~/.cargo/git/db/
          ~/.cargo/bin/
          target/
        key: v2-${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.toml') }}
